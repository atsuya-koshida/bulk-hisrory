= render 'shared/header'
.container.flex
  .container__inner
    .container__title 種目の編集
    .container__form
      = form_with model: @training_menu, local: true do |f|
        .form-group
          = f.label :title, '種目名'
          = f.text_field :title
        .form-group
          = f.label :part_id, '部位'
          = f.collection_select :part_id, Part.all, :id, :name
        .form-group
          = f.submit '更新する', class: 'submit-btn'
= render 'shared/footer'